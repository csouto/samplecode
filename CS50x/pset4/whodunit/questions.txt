1.  stdint.h is a header file that declares integer types with specified size.
2.  We will know the size of the integer precisely, across different plataforms.
3.  One, four, four and two.
4.  BM
5.  bfSize contains the size of the bitmap file and biSize contains the size of the header structure itself.
6.  Informations about pixels colors are stored in top-down/upper-left orientation.
7.  biBitCount
8.  Because file might be inaccessible.
9.  Because we're looking for the headers only and the exactly size was provided.
10. Three.
11. Adjusts the offset of the file pointer, can define a new starting bit.
12. Used in fseek as a third argument, allows to adjust a offset in relation to current location.
